export default class Movie {
    constructor(titulo, duracion, fecha_estreno, genero_id, url_imagen) {
        this.titulo = titulo,
        this.duracion = duracion,
        this.fecha_estreno = fecha_estreno,
        this.genero_id = genero_id,
        this.url_imagen = url_imagen
    }
}