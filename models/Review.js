export default class Review {
    constructor(usuario_id, pelicula_id, calificacion, comentario) {
        this.usuario_id = usuario_id,
        this.pelicula_id = pelicula_id,
        this.calificacion = calificacion,
        this.comentario = comentario
    }
}